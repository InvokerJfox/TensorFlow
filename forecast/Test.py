import numpy as np
import datetime as dt
import time
import pandas

# my_matrix = np.loadtxt(open('data/HZWeather.csv', 'r'), dtype=np.str)
# my_matrix = np.loadtxt('data/HZWeather.csv', dtype=np.str)
# print((config.FORECAST_START_DAY - config.FORECAST_END_DAY).days * 3)
# raise ValueError('FORECAST_END_DAY  < FORECAST_START_DAY')
# data = np.loadtxt('data/024.txt', delimiter=',', dtype=bytes).astype(str)
# print(data)
# ls = ymh(data, [0])  # args1"data, args:list of date
# ls.sort(key=lambda x: x[0])
# print(np.mat(ls)[:, 0])
# print('max:' + str(ls[-1][0]))  # 2016/10/7.
# print('min:' + str(ls[0]))  # 2012/9/2
# b = a[:, 1].astype(np.int32)
# print(b)
# data = np.loadtxt('data/024.txt', delimiter=',', dtype=bytes).astype(str)
# it1 = iter(data)
# it2 = iter(next(it1))
# it3 = iter(next(it2))
# print(next(it3))

# data = [[1, 'a'], [2, 'b']]
# print(data)
# tmp = np.mat(data)
# #  np.mat(arr) nparr[:, 0]
# print(type(tmp[0,0]))

# data = Data(config)

# data = np.loadtxt('data/024.txt', delimiter=',', dtype=bytes).astype(str)
# ls = ymh(data, [0])  # args1"data, args:list of date
# ls.sort(key=lambda x: x[0])
# # print(np.mat(ls)[:, 0])
# print('max:', ls[-1][0])  # 2016/10/7.
# train_start_date = d2t(config.TRAINING_START_DAY)
# train_end_data = d2t(config.TRAINING_END_DAY)
#
# tmp = list(x for x in ls if train_start_date <= x[0] <= train_end_data)
# print('min:', tmp[0][0])
# print('max:', tmp[-1][0])

# data = pp.load_data()
# pp.preprocess_data(data)
#
# print(data)

# train_start_date = d2t(config.TRAINING_START_DAY)
# train_end_data = d2t(config.TRAINING_END_DAY)

# n = dt.datetime.now()
# n+=dt.timedelta(1)
# print(n)

# a = [['a', 1, 3], ['b', 2, 4]]
# b = np.asarray(np.asarray(a)[:, 1:2], dtype=float)
# print(b)
# ma = np.mat(a)
# vector = np.asarray(a)[:, 1:2]
# print(b.T + 2)

# print(type(time.time()))
# dt.datetime.now()
# x = range(10)
# print(list(i for i in x))

# x = np.asarray([1, 2, 3, 4])
# y = x + [1]
# a + b * 1. / (1 + np.e ** (-c * x + d)))
# print(x[1:3])
# a = dt.datetime(2013, 3, 27)  # 学习数据的开始日期
# b = dt.datetime(2013, 4, 27)  # 学习数据的开始日期
#
# print((a - b).days)

# t = time.localtime()
# tt = dt.datetime.now()
list = range(1)
print(list[1])
